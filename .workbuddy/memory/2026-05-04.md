# 2026-05-04 工作记录

## 修复建筑文化页面多项问题

### 问题
1. 民居地域分布饼图数据全为1（region_distribution的value都是1）
2. 民居结构类型柱状图数据全为1（`detail.map(() => 1)`）
3. 皇宫规模图数据全为1（`detail.map(() => 1)`）
4. 桥梁结构类型饼图数据全为1（type_distribution的value都是1）
5. 桥梁世界之最年份解析有误（parseInt建造年代字符串失败）
6. 文化页面没有朝代筛选联动（其他4个页面都有）

### 修复内容
1. **民居地域分布饼图**：改用 `detail` 中的 `遗存数量_全国重点` 作为 value
2. **民居结构类型柱状图**：改用 `遗存数量_全国重点` 作为柱高
3. **民居材料构成饼图**：按材料类型累加 `遗存数量_全国重点`
4. **皇宫规模图**：从 `规模` 字段用正则提取面积数字（如"72万平方米"→72）
5. **皇宫现存宫殿图**：用真实面积数据计算保存完整度
6. **桥梁结构类型饼图**：改用 `总长_米` 作为 value
7. **桥梁世界之最图**：修复建造年代解析，用正则提取4位数年份
8. **朝代筛选联动**：
   - 新增 `getDynastyFilter()` 读取URL参数
   - 新增 `filterByDynasty()` 过滤文化数据中的detail数组
   - 新增 `matchDynasty()` 朝代关键词映射（支持六大朝代→子朝代匹配）
   - 页面标题显示当前筛选朝代

### 验证
- `npm run build` 构建成功 ✅

## 修复朝代筛选联动 + 添加筛选按钮（Task 40-42）

### 问题
1. 科学家页用联动筛选后图表全没了——URL参数是"隋唐"但数据中是"隋"、"唐"，精确匹配不上
2. 民居文化页没有朝代字段，筛选后数据全空
3. 首页和文化页没有筛选标签UI（其他3个页面有）
4. 民居饼图标题"地域分布"与实际展示的"遗存数量分布"不匹配

### 修复内容
1. **新增通用朝代匹配函数 `matchDynastyGroup()`**（utils.js）
   - 支持六大朝代分组→子朝代正则匹配
   - 如 `matchDynastyGroup("唐", "隋唐")` → true
   - 如 `matchDynastyGroup("明末清初", "明清")` → true

2. **修复科学家页筛选**（scientist/main.js）
   - 导入 matchDynastyGroup 替换精确匹配
   - graph_data 过滤改为用科学家名称匹配而非索引ID

3. **修复文化页筛选**（culture/main.js）
   - residence（民居）没有朝代字段，跳过朝代过滤
   - 其他tab使用 matchDynastyGroup 匹配

4. **添加筛选标签UI**（home/index.html + culture/index.html）
   - 在 header 下方添加 `#dynasty-filter` div
   - 包含朝代标签和取消筛选（×）按钮
   - 对齐其他3个页面的UI风格

5. **首页添加 showDynastyFilter**（home/main.js）
   - 导入 matchDynastyGroup
   - 新增筛选标签显示函数

6. **民居饼图标题修正**（culture/index.html）
   - "地域分布" → "遗存数量分布"

### 验证
- `npm run build` 构建成功 ✅

## 添加朝代筛选按钮组（Task 43）

### 问题
- 取消筛选后没有重新筛选的入口，用户无法再次筛选

### 修复
- 在5个子页面的 header 下方添加双状态筛选UI：
  - **无筛选时**：显示6个朝代按钮（先秦/秦汉/魏晋/隋唐/宋元/明清），点击跳转 `?dynasty=xxx`
  - **有筛选时**：显示当前筛选标签 + "×取消筛选" 链接
- 修改5个HTML添加 `.dynasty-btn` 样式和双状态div结构
- 修改5个main.js的 `showDynastyFilter` 函数控制显示/隐藏

## 改为按钮式动态筛选（Task 44）

### 改动
- 5个HTML：`<a>` 链接改为 `<button>`，添加 `.dynasty-btn.active` 高亮样式
- 5个main.js：
  - 新增 `initDynastyButtons()`：绑定按钮点击事件，toggle active状态
  - 新增 `reinitWithFilter(dynasty)`：销毁所有图表实例，重新加载数据
  - 原init()拆为 initData(dynasty) 可重复调用
  - 使用 `window.history.replaceState` 更新URL但不刷新页面
  - 点击已选中按钮取消筛选，恢复全部数据
- 无需页面跳转，纯前端动态切换

## 修复筛选bug + 支持多选筛选（Task 45）

### 修复
1. **传世著作页图表消失**：`mapBookDynasty` 函数不存在导致报错，改用 `matchDynastyGroup`
2. **文化页筛选不工作**：`initData` 函数分离出来，重新加载数据+过滤+初始化当前tab
3. **多选筛选**：所有5个页面改为支持多选
   - `classList.toggle('active')` 切换选中状态
   - URL参数用逗号分隔多朝代（如 `?dynasty=隋唐,宋元`）
   - `getDynastyFilter()` 返回数组
   - `initData` 接收数组，用 `filterDynasties.some(fd => matchDynastyGroup(...))` 匹配
   - `reinitWithFilter` 解析逗号分隔字符串为数组
